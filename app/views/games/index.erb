<h1>Played Games</h1>
<ul>
  <% @games.each do |game| %>
    <li><%= link_to game.is_single_player ? "Game #{game.id} - vs AI" : "Game #{game.id} - vs Player", game %></  li>
  <% end %>
</ul>
<p>
  <%= button_to "Back to Main Menu", :root, :method => "get", :class => "button" %>
</p>
